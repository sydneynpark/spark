[project]
name = "update-book-review"
version = "0.1.0"
description = "UpdateBookReview Lambda: syncs book review S3 objects into DynamoDB"
requires-python = ">=3.12"
dependencies = [
    "boto3",
    "python-frontmatter",
]
